{
    "build": {
      "install": "composer install --no-dev --optimize-autoloader && npm install",
      "build": "npm run build",
      "migrate": "php artisan migrate --force"
    },
    "start": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
  